import SwiftUI

@main
struct PulseDirectoryApp: App {
    private let container = DIContainer()

    var body: some Scene {
        WindowGroup {
            // Swap to DirectoryExerciseView() to practice View <-> VM task
            Text("PulseDirectory - Broken Interview Project (Skeleton)")
                .padding()
        }
    }
}
